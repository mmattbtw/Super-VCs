{
    "name": "pingpong",
    "version": "1.0.0",
    "description": "get a ping when a new voice channel gets created :^) (meant for use with a bot like auto voice channels.)",
    "main": "src/index.ts",
    "type": "module",
    "scripts": {
        "start": "tsc && node --experimental-modules --es-module-specifier-resolution=node build/index.js",
        "dev": "nodemon",
        "prod": "prisma generate && tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "mmatt <matt@mmatt.net>",
    "license": "MIT",
    "dependencies": {
        "discord.js": "^14.2.0",
        "dotenv": "^16.0.1",
        "ora": "^6.1.2",
        "topgg-autoposter": "^2.0.1"
    },
    "devDependencies": {
        "@prisma/client": "^4.2.1",
        "@types/node": "^20.5.1",
        "nodemon": "^2.0.19",
        "prisma": "^4.2.1",
        "ts-node": "^10.9.1",
        "typescript": "^5.0.2"
    }
}
